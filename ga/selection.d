module ga.selection;

import ga.individual;

void tournament(uint populationSize, uint genomeSize, T, uint numParticipants = 2)(
    const ref Population!(populationSize, genomeSize, T),
    const ref Population!(populationSize, genomeSize, T)) {

    {}
}
